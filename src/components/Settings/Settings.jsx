import styles from './Settings.module.css'

const Settings = () => {
    return (
    <main className={styles.settings}>
        <h2>Settings</h2>
    </main>
    );
} 

export default Settings;